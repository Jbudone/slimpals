Arrange the given gym equipment items on a 20x14 grid (x: 1-19, y: 1-13). Group items by category into clearly separated zones with at least 1 grid unit of empty space between different categories. Keep items within the same category clustered close together, within 2-3 grid units of each other. No two items should occupy the same or adjacent (within 1 unit) coordinates. Leave the center of the gym relatively open as a walking path.

This is an experimental preview only — it does not affect the live game's layout.

Respond with JSON only (no markdown, no explanation): a flat object mapping each given item key to its position, e.g. {"cardio_treadmill": {"x": 2, "y": 2}, "weights_dumbbells": {"x": 10, "y": 2}}.
